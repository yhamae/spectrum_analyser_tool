
def get_data(channel, freq, T, filename, mode)

	mode = d
	with open(filename) as f
		line = f.readlines()

	for i in len(line):
		if mode == "d":
			print(line[i])

		tmp = line[i].split()

		if len(tmp) > 2:
			continue

		channel = tmp1[0]
		freq = tmp1[1]
		T = tmp1[2]

		if tmp1[0].isalpha():
			continue
